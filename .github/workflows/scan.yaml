name: "Security Scan"

on:
  push:
    branches:
      - main # branch you want to trigger a scan on when updated
      - feat/security-scan

jobs:
  sonarqube:
    name: "Trigger Sonarqube Scan" # modify to suit project name
    uses: Xttreme/cross-github-actions/.github/workflows/sonarqube-ts.yaml@sonarqube
    with:
      runs-on: ubuntu-latest
      package-manager: yarn
    secrets: inherit